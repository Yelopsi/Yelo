const express = require('express');
const router = express.Router();

// Adicione aqui as rotas gerais ou extras que você extraiu do server.js
// Exemplo:
// router.get('/exemplo', (req, res) => { res.send('OK'); });

module.exports = router;
