<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Gerador de Banner LinkedIn | Yelo</title>
    <link rel="stylesheet" href="/css/01-variables.css">
    <style>
        /* Reset e Centralização da Página */
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color: #222; /* Fundo escuro para destacar o banner */
            font-family: var(--font-principal, sans-serif);
            overflow: hidden;
        }

        h2 { color: #fff; margin-bottom: 30px; font-weight: 300; font-family: var(--font-principal); }

        /* O Banner em si (Tamanho Exato do LinkedIn) */
        .linkedin-banner {
            width: 1584px;
            height: 396px;
            background-color: var(--verde-escuro, #1B4332);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 100px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.5);
            
            /* Ajuste de escala para visualização na tela */
            transform-origin: center;
        }

        /* Elementos Visuais de Fundo */
        .bg-shape {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px); /* Efeito Glow */
            opacity: 0.5;
        }
        .shape-1 {
            width: 500px; height: 500px;
            background: var(--cor-Yelo, #FFEE8C);
            top: -200px; right: -100px;
        }
        .shape-2 {
            width: 400px; height: 400px;
            background: #2E7D32; /* Verde um pouco mais claro */
            bottom: -150px; left: -100px;
        }

        /* Conteúdo Esquerda */
        .banner-content {
            z-index: 10;
            color: #fff;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .banner-logo {
            font-family: var(--font-titulos, serif);
            font-size: 5.5rem;
            color: var(--cor-Yelo, #FFEE8C);
            margin: 0;
            line-height: 1;
            text-shadow: 0 4px 20px rgba(0,0,0,0.2);
        }

        .banner-tagline {
            font-size: 1.8rem;
            font-weight: 300;
            opacity: 0.95;
            max-width: 700px;
            letter-spacing: 0.5px;
        }

        /* Conteúdo Direita */
        .banner-right {
            z-index: 10;
            text-align: right;
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            gap: 15px;
        }

        .banner-badge {
            background: rgba(255,255,255,0.15);
            border: 1px solid rgba(255,255,255,0.3);
            padding: 12px 35px;
            border-radius: 50px;
            font-size: 1.3rem;
            color: #fff;
            backdrop-filter: blur(10px);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .banner-site {
            font-size: 1.6rem;
            color: var(--cor-Yelo, #FFEE8C);
            font-weight: 700;
            letter-spacing: 1px;
        }

        /* Responsividade apenas para visualização (Zoom Out) */
        @media (max-width: 1700px) { .linkedin-banner { transform: scale(0.8); } }
        @media (max-width: 1300px) { .linkedin-banner { transform: scale(0.6); } }
        @media (max-width: 1000px) { .linkedin-banner { transform: scale(0.4); } }
    </style>
</head>
<body>

    <h2>Banner LinkedIn (1584 x 396px)</h2>

    <div class="linkedin-banner">
        <div class="bg-shape shape-1"></div>
        <div class="bg-shape shape-2"></div>

        <div class="banner-content">
            <h1 class="banner-logo">Yelo 💛</h1>
            <p class="banner-tagline">Conectando psicólogos e pacientes de forma simples, humana e acessível.</p>
        </div>

        <div class="banner-right">
            <div class="banner-badge">Saúde Mental para Todos</div>
            <span class="banner-site">www.yelopsi.com.br</span>
        </div>
    </div>

</body>
</html>