<!DOCTYPE html>
<html lang="pt-br">
<head>
    <title>Crie sua Conta - Yelo | Comece sua Terapia</title>
    <meta name="description" content="Cadastre-se gratuitamente na Yelo e encontre o psicólogo ideal. Agende sessões online ou presenciais com segurança e facilidade.">
    
    <meta property="og:title" content="Crie sua Conta na Yelo">
    <meta property="og:description" content="Junte-se à Yelo para cuidar da sua saúde mental. Cadastro rápido e gratuito.">

    <%- include('partials/head') %>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    
    <style>
        .login-container { max-width: 450px !important; margin-top: 60px; margin-bottom: 80px; }
        .form-group input { width: 100%; box-sizing: border-box; }

        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }
        .input-wrapper input {
            padding-right: 45px; /* Espaço para o ícone não cobrir o texto */
        }
        .btn-toggle-senha {
            position: absolute;
            right: 15px;
            background: none;
            border: none;
            cursor: pointer;
            color: #666;
            display: flex;
            padding: 0;
        }
        .btn-toggle-senha:hover { color: #1B4332; }
    </style>
</head>
<body class="pagina-login">

    <%- include('partials/header') %>

    <main class="login-main ruido">
        <div class="login-container">
            <h1 class="login-titulo">Criar conta</h1>
            <p style="text-align:center; margin-bottom:20px; color: #555;">
                Junte-se à Yelo para cuidar da sua saúde mental.
            </p>
            
            <form class="login-form" id="form-registro">
                <p id="mensagem-registro" class="mensagem-oculta" style="text-align: center; margin-bottom: 15px; font-weight: bold;"></p>
                
                <div class="form-group">
                    <label for="nome-completo">Nome Completo</label>
                    <input type="text" id="nome-completo" name="nome" placeholder="Seu nome" autocomplete="name" required>
                </div>

                <div class="form-group">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name="email" placeholder="seu@email.com" autocomplete="email" required>
                </div>

                <div class="form-group">
                    <label for="senha">Senha</label>
                    <div class="input-wrapper">
                        <input type="password" id="senha" name="senha" placeholder="Mínimo 6 caracteres" autocomplete="new-password" required minlength="6">
                        <button type="button" class="btn-toggle-senha" data-target="senha" title="Mostrar senha">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>
                        </button>
                    </div>
                </div>

                <div class="form-group">
                    <label for="confirmar-senha">Confirmar Senha</label>
                    <div class="input-wrapper">
                        <input type="password" id="confirmar-senha" name="confirmar-senha" placeholder="Repita a senha" autocomplete="new-password" required>
                        <button type="button" class="btn-toggle-senha" data-target="confirmar-senha" title="Mostrar senha">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>
                        </button>
                    </div>
                </div>

                <div class="form-group-checkbox" style="display: flex; gap: 10px; align-items: start; margin-top: 10px;">
                    <input type="checkbox" id="termos" name="termos" required style="margin-top: 4px;">
                    <label for="termos" style="font-size: 0.85rem; color: #666;">
                        Li e aceito os <a href="/termos" target="_blank" style="color: var(--verde-escuro); font-weight: bold;">Termos de Uso</a> e <a href="/privacidade" target="_blank" style="color: var(--verde-escuro); font-weight: bold;">Política de Privacidade</a>.
                    </label>
                </div>

                <button type="submit" class="btn-cta-principal" style="width: 100%; border: none; cursor: pointer; margin-top: 15px;">
                    Criar Conta Gratuita
                </button>
            </form>

            <div style="display: flex; align-items: center; margin: 20px 0;">
                <hr style="flex-grow: 1; border: 0; border-top: 1px solid #eee;">
                <span style="padding: 0 10px; color: #888; font-size: 0.9rem;">ou</span>
                <hr style="flex-grow: 1; border: 0; border-top: 1px solid #eee;">
            </div>

            <!-- Botão do Google -->
            <div id="g_id_onload"
                 data-client_id="283886540808-qj13i35cfagnp9rc6qou1o66mdv3ppkl.apps.googleusercontent.com"
                 data-callback="handleCredentialResponse">
            </div>
            <div class="g_id_signin"
                 data-type="standard"
                 data-size="large"
                 data-theme="outline"
                 data-text="signup_with"
                 data-shape="rectangular"
                 data-logo_alignment="left"
                 data-width="100%">
            </div>

            <div class="login-links" style="margin-top: 25px; text-align: center;">
                <a href="/login">Já tem uma conta? <strong>Faça Login</strong></a>
            </div>
            
            <hr style="margin: 30px 0; border: 0; border-top: 1px solid #eee;">
            
            <div style="text-align: center;">
                <p style="font-size: 0.9rem; color: #666; margin-bottom: 10px;">É psicólogo?</p>
                <a href="/psi_questionario.html" class="btn btn-outline" style="color: var(--verde-escuro); border-color: var(--verde-escuro);">Cadastrar como Profissional</a>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>
    
    <script src="/config.js"></script>
    <script src="/cadastro.js"></script>
    <script>
        // Callback do Google (Reutilizável)
        async function handleCredentialResponse(response) {
            try {
                const res = await fetch('/api/auth/google', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ token: response.credential })
                });

                const data = await res.json();

                if (res.ok) {
                    localStorage.setItem('Yelo_token', data.token);
                    localStorage.setItem('Yelo_user', JSON.stringify(data.user));
                    window.location.href = data.redirect;
                } else {
                    // Se der erro, mostra mensagem (ex: conta inativa)
                    const msgElement = document.getElementById('mensagem-registro');
                    if(msgElement) {
                        msgElement.textContent = data.error;
                        msgElement.className = 'mensagem-erro';
                        msgElement.style.display = 'block';
                    } else {
                        alert(data.error || 'Erro ao cadastrar com Google.');
                    }
                }
            } catch (error) {
                console.error('Erro Google Cadastro:', error);
                alert('Erro de conexão.');
            }
        }
    </script>
</body>
</html>