<!DOCTYPE html>
<html lang="pt-br">
<head>
    <title>Blog | Yelo</title>
    <meta name="description" content="Reflexões, psicoeducação e conteúdos sobre saúde mental.">
    <%- include('partials/head') %>
    
    <style>
    /* --- CONFIGURAÇÃO GERAL --- */
    .pagina-blog {
        padding-top: 140px; /* Garante que o menu verde não cubra o título */
        padding-bottom: 80px;
        background-color: #fdfaf6;
        min-height: 100vh;
    }

    .container-blog {
        max-width: 1200px; /* Largura maior para ficar elegante */
        margin: 0 auto;
        padding: 0 20px;
    }

    /* --- CABEÇALHO (HERO) --- */
    .blog-hero {
        text-align: center;
        margin-bottom: 50px;
    }
    .blog-hero h1 {
        font-family: 'New Kansas', serif;
        font-size: 3.5rem;
        color: #1B4332;
        margin: 0 0 10px 0;
    }
    .blog-hero p {
        font-family: 'Lato', sans-serif;
        font-size: 1.2rem;
        color: #666;
        margin: 0;
    }

    /* --- POST DESTAQUE (LARGURA TOTAL) --- */
    .post-destaque {
        display: flex; /* Força lado a lado */
        background: #fff;
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        margin-bottom: 60px;
        text-decoration: none;
        color: inherit;
        height: 450px; /* Altura fixa para impacto visual */
        border: 1px solid rgba(0,0,0,0.02);
    }
    
    .destaque-img-wrapper {
        flex: 1.2; /* Imagem ocupa 60% */
        height: 100%;
        overflow: hidden;
    }
    .destaque-img-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s;
    }
    .post-destaque:hover .destaque-img-wrapper img { transform: scale(1.03); }

    .destaque-conteudo {
        flex: 0.8; /* Texto ocupa 40% */
        padding: 60px;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .tag-destaque {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        font-weight: bold;
        color: #1B4332;
        margin-bottom: 20px;
        display: block;
    }

    .destaque-conteudo h2 {
        font-family: 'New Kansas', serif;
        font-size: 2.5rem;
        color: #1B4332;
        margin: 0 0 20px 0;
        line-height: 1.1;
    }

    .destaque-conteudo p {
        font-size: 1.1rem;
        color: #555;
        line-height: 1.6;
        margin-bottom: 30px;
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    /* --- GRID DE BAIXO (MANTIDO IGUAL) --- */
    .blog-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 30px; }
    .post-card { background: #fff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 15px rgba(0,0,0,0.03); transition: transform 0.3s; height: 100%; display: flex; flex-direction: column; }
    .post-card:hover { transform: translateY(-5px); }
    .post-card img { width: 100%; height: 220px; object-fit: cover; }
    .post-card-body { padding: 25px; flex: 1; display: flex; flex-direction: column; }
    .post-card-body h3 { font-family: 'New Kansas', serif; font-size: 1.4rem; color: #1B4332; margin: 10px 0; line-height: 1.3; }
    .post-card-body p { font-size: 0.95rem; color: #666; line-height: 1.5; margin-bottom: 20px; flex: 1; }
    .post-card-footer { border-top: 1px solid #eee; padding-top: 15px; display: flex; align-items: center; gap: 10px; margin-top: auto; }
    .post-card-footer img { width: 28px; height: 28px; border-radius: 50%; object-fit: cover; }
    .post-card-footer span { font-size: 0.85rem; color: #555; font-weight: 600; }

    /* MOBILE */
    @media (max-width: 900px) {
        .post-destaque { flex-direction: column; height: auto; }
        .destaque-img-wrapper { height: 250px; flex: none; }
        .destaque-conteudo { padding: 30px; flex: none; }
        .destaque-conteudo h2 { font-size: 2rem; }
    }
    </style>
</head>
<body>

<%- include('partials/header') %>

<main class="pagina-blog">
    
    <div class="container-blog">
        
        <header class="blog-hero">
            <h1>Blog Yelo</h1>
            <p>Reflexões e ferramentas para sua jornada de autoconhecimento.</p>
        </header>

        <% if (posts && posts.length > 0) { %>
            
            <% const destaque = posts[0]; %>
            <a href="/blog/post/<%= destaque.id %>" class="post-destaque">
                <div class="destaque-img-wrapper">
                    <img src="<%= destaque.imagem_url || 'https://images.pexels.com/photos/4101143/pexels-photo-4101143.jpeg?auto=compress&cs=tinysrgb&w=1260' %>" alt="<%= destaque.titulo %>">
                </div>
                <div class="destaque-conteudo">
                    <span class="tag-destaque">MAIS RECENTE</span>
                    <h2><%= destaque.titulo %></h2>
                    <p><%= destaque.conteudo.substring(0, 180) %>...</p>
                    
                    <div class="post-card-footer" style="border:none; padding:0;">
                        <img src="<%= destaque.autor && destaque.autor.fotoUrl ? formatImageUrl(destaque.autor.fotoUrl) : 'https://placehold.co/40x40' %>" alt="Autor">
                        <span>Psi. <%= destaque.autor ? destaque.autor.nome.split(' ')[0] : 'Yelo' %></span>
                    </div>
                </div>
            </a>

            <% const outros = posts.slice(1); %>
            <% if (outros.length > 0) { %>
                <div class="blog-grid">
                    <% outros.forEach(post => { %>
                        <article class="post-card">
                            <a href="/blog/post/<%= post.id %>" class="card-link">
                                <img src="<%= post.imagem_url || 'https://images.pexels.com/photos/4101143/pexels-photo-4101143.jpeg?auto=compress&cs=tinysrgb&w=600' %>" alt="<%= post.titulo %>" class="card-img">
                                <div class="post-card-body">
                                    <span class="card-date">
                                        <%= new Date(post.created_at).toLocaleDateString('pt-BR', { day: 'numeric', month: 'short' }) %>
                                    </span>
                                    <h3><%= post.titulo %></h3>
                                    <p><%= post.conteudo.substring(0, 100) %>...</p>
                                    
                                    <div class="post-card-footer">
                                        <img src="<%= post.autor && post.autor.fotoUrl ? formatImageUrl(post.autor.fotoUrl) : 'https://placehold.co/30x30' %>" alt="Autor">
                                        <span>Psi. <%= post.autor ? post.autor.nome.split(' ')[0] : 'Yelo' %></span>
                                    </div>
                                </div>
                            </a>
                        </article>
                    <% }) %>
                </div>
            <% } %>

        <% } else { %>
            <div style="text-align: center; padding: 80px 0; color: #888;">
                <p style="font-size: 1.2rem;">Ainda não temos artigos publicados.</p>
                <p>Volte em breve para conferir novidades.</p>
            </div>
        <% } %>

    </div>

</main>

<%- include('partials/footer') %>

</body>
</html>