<!DOCTYPE html>
<html lang="pt-br">
<head>
    <title>Blog | Yelo</title>
    <meta name="description" content="Reflexões, psicoeducação e conteúdos sobre saúde mental.">
    <%- include('partials/head') %>
    <style>
        /* ESTILOS DE RECUPERAÇÃO DO DESIGN */
        .pagina-blog { background-color: #fdfaf6; padding-top: 100px; padding-bottom: 80px; min-height: 100vh; }

        .blog-hero { text-align: center; margin-bottom: 60px; padding: 40px 20px; }
        .blog-hero h1 { font-family: 'New Kansas', serif; font-size: 3.5rem; color: #1B4332; margin-bottom: 15px; }
        .blog-hero p { font-family: 'Lato', sans-serif; font-size: 1.2rem; color: #666; }

        .blog-container { max-width: 1100px; margin: 0 auto; padding: 0 20px; }

        /* Card Destaque */
        .post-destaque {
            display: grid; grid-template-columns: 1.2fr 1fr;
            background: #fff; border-radius: 20px; overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05); margin-bottom: 60px;
            text-decoration: none; color: inherit; min-height: 400px;
            transition: transform 0.3s;
        }
        .post-destaque:hover { transform: translateY(-5px); }
        .post-destaque img { width: 100%; height: 100%; object-fit: cover; }
        .post-destaque-conteudo { padding: 40px; display: flex; flex-direction: column; justify-content: center; }
        .post-destaque-conteudo h2 { font-family: 'New Kansas', serif; font-size: 2rem; color: #1B4332; margin: 15px 0; line-height: 1.2; }
        .tag-destaque { background: #FFEE8C; color: #1B4332; padding: 5px 10px; border-radius: 15px; font-weight: bold; font-size: 0.8rem; text-transform: uppercase; align-self: flex-start; }

        /* Grid */
        .blog-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 30px; }
        .post-card { background: #fff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 15px rgba(0,0,0,0.05); transition: transform 0.3s; }
        .post-card:hover { transform: translateY(-5px); }
        .post-card img { width: 100%; height: 200px; object-fit: cover; }
        .post-card-body { padding: 20px; }
        .post-card-body h3 { font-family: 'New Kansas', serif; font-size: 1.4rem; color: #1B4332; margin: 10px 0; }
        .post-card-body p { font-size: 0.95rem; color: #666; line-height: 1.5; }
        .autor { display: flex; align-items: center; gap: 10px; margin-top: 15px; padding-top: 15px; border-top: 1px solid #eee; }
        .autor img { width: 30px; height: 30px; border-radius: 50%; object-fit: cover; }
        .autor span { font-size: 0.85rem; color: #555; font-weight: bold; }

        @media (max-width: 768px) {
            .post-destaque { grid-template-columns: 1fr; }
            .post-destaque img { height: 250px; }
            .blog-hero h1 { font-size: 2.5rem; }
        }
    </style>
</head>
<body>

<%- include('partials/header') %>

<main class="pagina-blog ruido">

    <!-- HERO -->
    <section class="blog-hero">
        <h1>Blog Yelo</h1>
        <p>Reflexões clínicas, sem autoajuda rasa.</p>
    </section>

    <section class="blog-container">

        <% if (posts && posts.length > 0) { %>
        <% const destaque = posts[0]; %>

        <!-- POST DESTAQUE -->
        <a href="/blog/post/<%= destaque.id %>" class="post-destaque">
            <img
                src="<%= destaque.imagem_url || 'https://images.pexels.com/photos/4101143/pexels-photo-4101143.jpeg' %>"
                alt="<%= destaque.titulo %>"
            />

            <div class="post-destaque-conteudo">
                <span class="tag-destaque">Mais recente</span>

                <h2><%= destaque.titulo %></h2>

                <p>
                    <%= destaque.conteudo.substring(0, 180) %>...
                </p>

                <div class="autor">
                    <img src="<%= destaque.autor?.fotoUrl ? formatImageUrl(destaque.autor.fotoUrl) : 'https://placehold.co/40x40' %>">
                    <span>Psi. <%= destaque.autor ? destaque.autor.nome.split(' ')[0] : 'Yelo' %></span>
                </div>
            </div>
        </a>

        <!-- GRID -->
        <% const outros = posts.slice(1); %>
        <% if (outros.length > 0) { %>

        <div class="blog-grid">
            <% outros.forEach(post => { %>
            <article class="post-card">
                <a href="/blog/post/<%= post.id %>" style="text-decoration: none; color: inherit;">
                    <img
                        src="<%= post.imagem_url || 'https://images.pexels.com/photos/4101143/pexels-photo-4101143.jpeg' %>"
                        alt="<%= post.titulo %>"
                    />

                    <div class="post-card-body">
                        <span class="data">
                            <%= new Date(post.created_at).toLocaleDateString('pt-BR', { day: 'numeric', month: 'short' }) %>
                        </span>

                        <h3><%= post.titulo %></h3>

                        <p>
                            <%= post.conteudo.substring(0, 120) %>...
                        </p>

                        <div class="autor">
                            <img src="<%= post.autor?.fotoUrl ? formatImageUrl(post.autor.fotoUrl) : 'https://placehold.co/30x30' %>">
                            <span>Psi. <%= post.autor ? post.autor.nome.split(' ')[0] : 'Yelo' %></span>
                        </div>
                    </div>
                </a>
            </article>
            <% }) %>
        </div>

        <% } %>
        <% } else { %>

        <div class="blog-vazio">
            <p>Nenhum artigo publicado ainda.</p>
        </div>

        <% } %>

    </section>

    <!-- CTA -->
    <section class="cta-blog">
        <h2>Conteúdo ajuda. Terapia transforma.</h2>
        <p>Quando quiser ir além da leitura, a gente te conecta.</p>
        <a href="/questionario.html" class="btn-cta-principal">Encontrar meu terapeuta</a>
    </section>

    <%- include('partials/aviso-crise') %>

</main>

<%- include('partials/footer') %>

</body>
</html>
